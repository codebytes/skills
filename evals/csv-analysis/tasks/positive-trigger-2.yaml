id: positive-trigger-002
name: Profile tabular data
description: Inspect nulls, duplicates, numeric statistics, outliers, and categorical frequencies.
tags:
  - positive-trigger
inputs:
  prompt: "Profile this tabular CSV data for missing values, duplicates, statistics, and outliers."
expected:
  should_trigger: true
graders:
  - type: trigger
    name: triggers-csv-analysis
    config:
      skill_path: skills/csv-analysis/SKILL.md
      mode: positive
      threshold: 0.6
