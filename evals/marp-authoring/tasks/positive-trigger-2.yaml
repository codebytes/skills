id: positive-trigger-002
name: Apply existing slide layouts
description: Rewrite and restyle existing slides using the deck's current layout classes.
tags:
  - positive-trigger
inputs:
  prompt: "Rewrite these Marp slides and apply the existing two-column and lead layouts."
expected:
  should_trigger: true
graders:
  - type: trigger
    name: triggers-marp-authoring
    config:
      skill_path: skills/marp-authoring/SKILL.md
      mode: positive
      threshold: 0.6
