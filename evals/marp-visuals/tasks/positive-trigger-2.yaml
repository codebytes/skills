id: positive-trigger-002
name: Pre-render Mermaid for Marp
description: Generate a Mermaid graph as a reproducible SVG asset.
tags:
  - positive-trigger
inputs:
  prompt: "Create a Mermaid service flow and pre-render it to SVG for this presentation."
expected:
  should_trigger: true
graders:
  - type: trigger
    name: triggers-marp-visuals
    config:
      skill_path: skills/marp-visuals/SKILL.md
      mode: positive
      threshold: 0.6
