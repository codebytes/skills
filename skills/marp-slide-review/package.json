{
  "name": "marp-slide-review",
  "version": "1.0.0",
  "private": true,
  "description": "Development tooling for the marp-slide-review skill.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22.20.0"
  },
  "scripts": {
    "overflow": "node scripts/check-overflow.mjs",
    "render": "node scripts/render-review.mjs",
    "test": "node --test test/*.test.mjs",
    "eval:lint": "vally lint --eval-spec evals/marp-slide-review/eval.yaml --strict",
    "eval": "vally eval --eval-spec evals/marp-slide-review/eval.yaml --skill-dir ."
  },
  "devDependencies": {
    "@marp-team/marp-cli": "4.5.1",
    "playwright": "1.63.0"
  }
}
